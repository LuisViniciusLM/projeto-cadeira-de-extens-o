# .gitignore na RAIZ (se já tiver, só confira que contém estas linhas)
@"
node_modules/
dist/
build/
*.log
.env
.env.*
!*.example
"@ | Out-File -Encoding utf8 .gitignore

# apagar dependências locais (não versionar)
rd /s /q backend\node_modules 2>$null
rd /s /q frontend\node_modules 2>$null
