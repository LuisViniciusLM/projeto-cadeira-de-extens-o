# -------------------------------
# Servidor
# -------------------------------
NODE_ENV=development
PORT=4000

# Origem do frontend (CORS)
# - Local dev (Vite): http://localhost:5173
# - Produção (Vercel/Render): https://seu-front.vercel.app
FRONTEND_ORIGIN=http://localhost:5173

# -------------------------------
# Banco de dados (PostgreSQL)
# -------------------------------
# Exemplo Render/Neon (vem com sslmode=require):
# DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require
DATABASE_URL=

# Ajustes opcionais do pool
PG_POOL_MAX=10
PG_IDLE_TIMEOUT=30000
# Força SSL local caso necessário (normalmente deixe vazio)
# PG_SSL=true

# -------------------------------
# Segurança (MVP)
# -------------------------------
# Se vazio, o middleware authRequired libera o acesso (MVP).
# Se setado, exige header: x-api-key: <API_KEY>
API_KEY=

# -------------------------------
# Migração automática local
# -------------------------------
# Se "true", ao iniciar localmente o server tentará rodar schema.sql.
# Em produção, mantenha vazio/false.
RUN_MIGRATIONS=false
